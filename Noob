--// Meow Script (phiên bản màu tím, đếm % ngẫu nhiên, hoàn thành ~15s)

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local UIS = game:GetService("UserInputService")

if _G.__PHATLEE_HUB then _G.__PHATLEE_HUB:Destroy() end

local function toast(parent, title, msg, dur)
	dur = dur or 3
	local t = Instance.new("TextLabel")
	t.BackgroundColor3 = Color3.fromRGB(120, 100, 240)
	t.TextColor3 = Color3.fromRGB(255, 255, 255)
	t.Font = Enum.Font.GothamSemibold
	t.TextSize = 16
	t.Text = title.." - "..msg
	t.AnchorPoint = Vector2.new(0.5,1)
	t.Position = UDim2.new(0.5,0,0,-10)
	t.Parent = parent
	Instance.new("UICorner", t).CornerRadius = UDim.new(0,8)
	task.delay(dur, function()
		if t then t:Destroy() end
	end)
end

local function dragify(handle, target)
	local dragging, startPos, startInputPos
	handle.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			startPos = target.Position
			startInputPos = input.Position
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then dragging = false end
			end)
		end
	end)
	UIS.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			local delta = input.Position - startInputPos
			target.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
	end)
end

-- GUI chính
local gui = Instance.new("ScreenGui")
gui.Name = "MeowScript"
gui.Parent = CoreGui
gui.ZIndexBehavior = Enum.ZIndexBehavior.Global
_G.__PHATLEE_HUB = gui

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0,440,0,240)
frame.Position = UDim2.new(0.5,-220,0.5,-120)
frame.BackgroundColor3 = Color3.fromRGB(25, 26, 34)
frame.BorderSizePixel = 0
Instance.new("UICorner", frame).CornerRadius = UDim.new(0,16)
local fs = Instance.new("UIStroke", frame)
fs.Color = Color3.fromRGB(85,85,95)
fs.Thickness = 1

local titleBar = Instance.new("Frame", frame)
titleBar.Size = UDim2.new(1,0,0,48)
titleBar.BackgroundColor3 = Color3.fromRGB(28,30,36)
titleBar.BorderSizePixel = 0
Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0,16)
local title = Instance.new("TextLabel", titleBar)
title.BackgroundTransparency = 1
title.Size = UDim2.new(1,-120,1,0)
title.Position = UDim2.new(0,16,0,0)
title.Font = Enum.Font.GothamBlack
title.TextSize = 20
title.TextXAlignment = Enum.TextXAlignment.Left
title.TextColor3 = Color3.fromRGB(235,235,245)
title.Text = "Meow Script"
dragify(titleBar, frame)

local grid = Instance.new("Frame", frame)
grid.BackgroundTransparency = 1
grid.Size = UDim2.new(1,-24,1,-84)
grid.Position = UDim2.new(0,12,0,56)
local layout = Instance.new("UIGridLayout", grid)
layout.CellPadding = UDim2.new(0,12,0,12)
layout.CellSize = UDim2.new(0.5,-6,0,46)
layout.FillDirectionMaxCells = 2
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
layout.VerticalAlignment = Enum.VerticalAlignment.Top

-- 🧠 Nhân bản v2 (loadstring) với đếm ngược 120s
local btnClone2 = pill(grid, "🧠 Nhân bản v2")
pillColor(btnClone2, 114,106,240)
btnClone2.MouseButton1Click:Connect(function()
	local countdown = 120
	btnClone2.Active = false
	task.spawn(function()
		while countdown > 0 do
			btnClone2.Text = "Wait "..countdown.."s"
			pillColor(btnClone2, 200,140,50)
			wait(1)
			countdown = countdown - 1
		end
		btnClone2.Text = "Đang nhân bản..."
		pillColor(btnClone2, 70,200,90)
		toast(frame, "Nhân bản v2", "Đang nhân bản...", 2)
		pcall(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/tunadan212/Kkkk/refs/heads/main/K"))()
		end)
		btnClone2.Active = true
		btnClone2.Text = "🧠 Nhân bản v2"
		pillColor(btnClone2, 114,106,240)
	end)
end)


-- Footer: Script by PhatLee | Enjoy It
local footer = Instance.new("TextLabel", frame)
footer.BackgroundTransparency = 1
footer.Size = UDim2.new(1, -20, 0, 24)
footer.Position = UDim2.new(0, 10, 1, -26)
footer.Font = Enum.Font.Gotham
footer.TextSize = 14
footer.TextColor3 = Color3.fromRGB(150,150,160)
footer.Text = "Meow Script | Enjoy It"
footer.TextXAlignment = Enum.TextXAlignment.Center

-- Nút Panel (Toggle)
local toggle = Instance.new("ImageButton", gui)
toggle.Size = UDim2.new(0,64,0,64)
toggle.AnchorPoint = Vector2.new(1,1)
toggle.Position = UDim2.new(1,-16,1,-16)
toggle.BackgroundColor3 = Color3.fromRGB(28,30,36)
toggle.Image = "rbxassetid://123085363160594"
Instance.new("UICorner", toggle).CornerRadius = UDim.new(1,0)
local ts = Instance.new("UIStroke", toggle)
ts.Color = Color3.fromRGB(84,130,255)
ts.Thickness = 1.2

dragify(toggle, toggle)
local visible = true
toggle.MouseButton1Click:Connect(function()
	visible = not visible
	frame.Visible = visible
end)

toast(frame, "Meơw Script", "UI đã tải!", 2)
